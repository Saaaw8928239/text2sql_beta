📋 Описание проекта
Text2SQL — это веб-приложение, которое позволяет пользователям формулировать запросы на естественном русском языке и автоматически преобразовывать их в SQL-запросы, которые выполняются в базе данных PostgreSQL. Система использует модели машинного обучения для понимания пользовательских запросов и генерации корректного SQL-кода.

# 1. Предварительные требования
Python 3.9+

PostgreSQL 12+

pip (менеджер пакетов Python)

Git (для клонирования репозитория)

# 2. Клонирование репозитория

git clone repository-url

cd text2sql-system
# 3. Настройка базы данных PostgreSQL

1. Подключитесь к PostgreSQL

psql -U postgres

2. Создайте базу данных

CREATE DATABASE company_db;

3. Выйдите из psql и импортируйте данные

\q

psql -U postgres -d company_db -f data/create_db.sql
# 4. Настройка виртуального окружения Python

1. Создание виртуального окружения

python -m venv venv

2. Активация (Windows)

venv\Scripts\activate

3. Активация (Linux/Mac)

source venv/bin/activate

# 5. Установка зависимостей

pip install -r requirements.txt

# 6. Настройка переменных окружения

Создайте файл .env в корневой директории (или отредактируйте существующий):

1. НАСТРОЙКИ POSTGRESQL

DB_HOST=localhost

DB_PORT=5432

DB_NAME=company_db

DB_USER=postgres

DB_PASSWORD=your_password

3. НАСТРОЙКИ FLASK

SECRET_KEY=dev-secret-key-for-coursework-2024

# 7. Запуск приложения
python app.py
